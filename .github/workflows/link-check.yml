name: "Link check"

on:
  schedule:
    - cron: "0 3 * * *" # 03:00 UTC daily
  workflow_dispatch:

jobs:
  lychee:
    name: "lychee"
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: lycheeverse/lychee-action@e7477775783ea5526144ba13e8db5eec57747ce8 # v2.9.0
        with:
          # --method get, not the default HEAD: some hosts error on HEAD but answer GET.
          # theqlforum.com (incl. its qlwiki. subdomain) 403s every automated checker, UA or no UA -
          # it's a real, human-reachable site, just not link-checker-reachable.
          # --accept-timeouts: archive.org's dn760101.eu mirror node (COMAL Reference Guide, cited
          # from 3 docs) is a live, working URL that's just consistently slow/flaky from GitHub's
          # runner network - report timeouts, don't fail the job over them, but still fail on any
          # other error (403, DNS, etc.) for that same URL.
          args: --no-progress --method get --max-concurrency 4 --accept-timeouts --exclude 'theqlforum\.com' "docs/research/**/*.md" "docs/adr/**/*.md"
          fail: true
